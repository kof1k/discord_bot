# 🎵 NightFall Music Bot

Музыкальный бот для Discord с поддержкой YouTube и SoundCloud.

## 📋 Описание

Бот использует собственный парсер на Python (`yt-dlp`) для скачивания аудио, что обеспечивает стабильную работу без зависимости от сломанных npm-библиотек.

## ✨ Возможности

- 🎶 Воспроизведение музыки с YouTube и SoundCloud
- 📜 Очередь воспроизведения
- 🔁 Повтор очереди
- 🔀 Перемешивание треков
- ⏸️ Пауза и продолжение
- ⏭️ Пропуск треков
- 📊 Отображение текущего трека

## 🛠️ Требования

- [Node.js](https://nodejs.org/) v18+
- [Python](https://www.python.org/) 3.8+
- [FFmpeg](https://ffmpeg.org/)

## 📦 Установка

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/your-username/nightfall-music-bot.git
cd nightfall-music-bot
```

### 2. Установите зависимости Node.js

```bash
npm install
```

### 3. Установите зависимости Python

```bash
pip install yt-dlp
```

### 4. Установите FFmpeg

**Windows (через Chocolatey):**
```powershell
choco install ffmpeg -y
```

**Windows (через winget):**
```powershell
winget install FFmpeg
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt install ffmpeg
```

**macOS:**
```bash
brew install ffmpeg
```

### 5. Настройте переменные окружения

Создайте файл `.env` в корне проекта:

```env
TOKEN=ваш_токен_бота
```

### 6. Получите токен бота

1. Перейдите на [Discord Developer Portal](https://discord.com/developers/applications)
2. Создайте новое приложение
3. Перейдите в раздел **Bot**
4. Нажмите **Reset Token** и скопируйте токен
5. Включите **Message Content Intent** в настройках бота

### 7. Пригласите бота на сервер

1. Перейдите в раздел **OAuth2** → **URL Generator**
2. Выберите scopes: `bot`, `applications.commands`
3. Выберите permissions:
   - Read Messages/View Channels
   - Send Messages
   - Embed Links
   - Connect
   - Speak
   - Use Voice Activity
4. Скопируйте сгенерированную ссылку и откройте в браузере

## 🚀 Запуск

```bash
node index.js
```

## 📝 Команды

| Команда | Алиас | Описание |
|---------|-------|----------|
| `!play <запрос>` | `!p` | Воспроизвести трек по названию или ссылке |
| `!pause` | - | Поставить на паузу |
| `!resume` | - | Продолжить воспроизведение |
| `!skip` | `!s` | Пропустить текущий трек |
| `!stop` | - | Остановить воспроизведение и очистить очередь |
| `!queue` | `!q` | Показать очередь воспроизведения |
| `!nowplaying` | `!np` | Показать текущий трек |
| `!shuffle` | - | Перемешать очередь |
| `!loop` | - | Включить/выключить повтор очереди |
| `!leave` | `!disconnect` | Отключить бота от голосового канала |
| `!help` | `!h` | Показать список команд |

## 💡 Примеры использования

```
!play Never Gonna Give You Up
!play https://www.youtube.com/watch?v=dQw4w9WgXcQ
!play https://soundcloud.com/artist/track
!skip
!queue
```

## 📁 Структура проекта

```
nightfall-music-bot/
├── index.js          # Основной файл бота
├── downloader.py     # Python скрипт для скачивания аудио
├── package.json      # Зависимости Node.js
├── .env              # Переменные окружения (не коммитить!)
├── .gitignore        # Игнорируемые файлы
├── README.md         # Документация
└── downloads/        # Папка для временных аудиофайлов
```

## 📦 Зависимости

### Node.js
- discord.js
- @discordjs/voice
- @snazzah/davey
- opusscript
- libsodium-wrappers
- ffmpeg-static
- dotenv

### Python
- yt-dlp

## ⚙️ Конфигурация

| Переменная | Описание |
|------------|----------|
| `TOKEN` | Токен Discord бота |
| `PREFIX` | Префикс команд (по умолчанию `!`) |

## 🐛 Решение проблем

### Бот не воспроизводит музыку
1. Проверьте установлен ли FFmpeg: `ffmpeg -version`
2. Проверьте установлен ли yt-dlp: `yt-dlp --version`
3. Убедитесь что бот имеет права на подключение к голосовому каналу

### Ошибка "FFMPEG_NOT_INSTALLED"
Установите FFmpeg глобально или через `ffmpeg-static`

### Ошибка скачивания
Обновите yt-dlp:
```bash
pip install -U yt-dlp
```

## 📄 Лицензия

MIT License

## 👤 Автор

NightFall

---

⭐ Если проект понравился, поставьте звезду на GitHub!
